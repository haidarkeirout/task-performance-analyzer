# EXAMPLE ONLY. Replace values in Streamlit Settings > Secrets.
# Do not commit real credentials or change this example into a credentials file.
APP_USERNAME = "<choose the application username>"
APP_PASSWORD = "<choose the application password>"
JIRA_BASE_URL = "https://your-site.atlassian.net"
JIRA_EMAIL = "<the connected Jira account email>"
JIRA_API_TOKEN = "<the API token for that account>"

# Optional settings; uncomment only when needed.
# JIRA_CLOUD_ID = "<cloud ID for a scoped API token>"
# JIRA_START_DATE_FIELD_ID = "customfield_10015"
# SOURCE_TIMEZONE = "Asia/Damascus"
# Optional ClickUp connection (personal token starts with pk_).
# CLICKUP_API_TOKEN = "pk_..."
# CLICKUP_WORKSPACE_ID = "90122015472"
